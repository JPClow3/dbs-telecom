# Taste
- For large multi-part tasks (e.g., long categorized bug-fix/audit lists), dispatch parallel subagents — one per category/goal — instead of handling everything sequentially. Explicitly requested ("use subagentes with /goal for each of them"). Group assignments so agents don't edit overlapping files. Confidence: 0.9
- When asked to "double check" work, re-verify from scratch instead of re-summarizing: re-run typecheck/test suites and confirm each fix directly on disk (read/grep the actual files), don't just trust earlier reports or subagent summaries. Confidence: 0.8
- When intentional behavior changes (e.g., security hardening) break pre-existing tests, update the tests rather than reverting the behavior: change only the assertions that encoded the old insecure behavior, keep each test's intent, and make tests self-contained (e.g., mint tokens with the current session version instead of relying on test-ordering side effects or poking private module state). Confidence: 0.75
- For tricky logic (e.g., SQL placeholder scanning, parsers), validate with a quick standalone sanity script asserting edge cases before accepting the implementation — this approach caught a real jsonb-operator gap during a double-check pass. Confidence: 0.7
- This user's projects use Portuguese for code comments and user-facing error messages; match the existing language convention when editing these codebases. Confidence: 0.75
- For review/audit requests, give blunt, criticism-only output: no compliments, strengths, or filler — just what needs fixing/improving/adding. Explicitly requested ("no compliments just stuff that needs fixing or improving or adding"). Structure findings grouped by severity (CRITICAL/HIGH/MEDIUM/LOW) with file:line evidence per item, and close with a short prioritized "attack first" themes summary. Confidence: 0.85
